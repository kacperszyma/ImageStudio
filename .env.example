# Copy to .env and fill in. NEVER commit the real .env.
ConnectionStrings__Postgres=Host=localhost;Port=5432;Database=imagestudio;Username=postgres;Password=postgres
# Inference provider (start with one)
FAL_API_KEY=
# Stripe (test mode)
STRIPE_SECRET_KEY=
# Identity (Entra External ID)
ENTRA_TENANT_ID=
ENTRA_CLIENT_ID=
